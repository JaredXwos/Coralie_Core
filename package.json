{
  "name": "nostr-webrtc-mesh",
  "version": "0.0.0",
  "description": "Fast, lightweight peer to peer messaging via WebRTC over Nostr. Gossip-based peer discovery, no central roster, minimal handshake overhead.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs",
      "require": "./dist/index.cjs"
    }
  },
  "main": "./dist/index.cjs",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest watch",
    "test:e2e": "playwright test",
    "typecheck": "tsc --noEmit",
    "build": "tsup",
    "test:integration": "playwright test src/integration.spec.ts --headed",
    "test:integration:headless": "playwright test src/integration.spec.ts"
  },
  "keywords": [
    "nostr",
    "webrtc",
    "mesh",
    "p2p",
    "networking"
  ],
  "license": "MIT",
  "dependencies": {
    "nostr-tools": "^2.23.12"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.1.1",
    "esbuild": "^0.28.1",
    "tsup": "^8.5.1",
    "typescript": "^5.6.3",
    "vitest": "^4.1.10"
  },
  "allowScripts": {
    "esbuild@0.28.1": true
  },
  "overrides": {
    "esbuild": "^0.28.1"
  }
}